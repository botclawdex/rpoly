# rPoly - Polymarket BTC 5m Trading Bot
# Copy to .env and fill in your values

# === WALLET ===
# MetaMask EOA private key (controls the Gnosis Safe proxy)
POLY_PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# Polymarket Gnosis Safe proxy address (visible on polymarket.com profile)
POLY_PROXY_ADDRESS=0xYOUR_PROXY_ADDRESS_HERE

# === POLYMARKET API CREDENTIALS ===
# Generated via ClobClient.deriveApiKey() using L1 auth
POLY_API_KEY=your-api-key-uuid
POLY_API_SECRET=your-base64url-secret=
POLY_API_PASSPHRASE=your-hex-passphrase

# === AUTH ===
# Password to protect trading endpoints (only needed in live mode)
RPOLY_AUTH_TOKEN=your-secret-token-here

# === MODE ===
# "live"     = full trading enabled (requires auth token)
# "readonly" = dashboard only, no trading (safe for public Vercel deploy)
RPOLY_MODE=readonly

# === SERVER ===
PORT=3001
